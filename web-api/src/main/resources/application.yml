spring:
  application:
    name: sf-movies
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  profiles:
    active: @spring.profiles.active@
  liquibase:
    enabled: true
    change-log: classpath:liquibase/master.xml

server:
  port: 8081